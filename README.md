# YouTube Channel Report

브라우저에서 CSV를 업로드해 유튜브 채널 성과를 보고서 형태로 확인하는 정적 HTML 도구입니다.

## 사용 방법

1. 웹페이지를 엽니다.
2. `표 데이터.csv`를 업로드합니다.
3. 선택 사항으로 `총계.csv`, `차트 데이터.csv`를 함께 업로드하면 추가 차트가 표시됩니다.

CSV는 서버에 저장하지 않고 사용자의 브라우저 안에서만 읽습니다.
